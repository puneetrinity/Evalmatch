/**
 * Mock for static file imports (images, fonts, etc.)
 * Used by Jest to handle asset imports in UI tests
 */

// Default export for static file imports
export default 'test-file-stub';

// Named export for React components (SVGs imported as ReactComponent)
export const ReactComponent = 'svg';

// Additional named exports that might be used
export const src = 'test-file-stub';
export const url = 'test-file-stub';